- move everything from Registers to instructions.c (gets rid of struct entirely!)
- combine add, mult, div instructions into one function; then use switch statement?
- global r_a, r_b, r_c and val_a, val_b, val_c
- 